# Auto-generated protobuf stubs — regenerated by `npm run proto:gen`.
# Prettier formatting here loses on every regen; the proto-drift CI job
# enforces a clean diff between committed stubs and fresh codegen.
src/providers/payerDaemon/gen/**

# Drizzle migration SQL — Postgres-flavored, not JS.
migrations/*.sql
migrations/meta/**

# Vendored / copied references
docs/references/*.pdf

# Build output
dist/
coverage/
node_modules/
