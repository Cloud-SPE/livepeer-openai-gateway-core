{
  "name": "@cloudspe/livepeer-gateway-core-minimal-shell",
  "version": "0.0.0",
  "private": true,
  "description": "Smallest runnable wiring of @cloudspe/livepeer-gateway-core. InMemoryWallet + no-op AuthResolver + the two daemon sidecars.",
  "type": "module",
  "scripts": {
    "start": "node --experimental-strip-types start.ts",
    "compose:up": "docker compose up",
    "compose:down": "docker compose down -v"
  },
  "dependencies": {
    "@cloudspe/livepeer-gateway-core": "^0.1.0",
    "fastify": "^4.29.1"
  }
}
